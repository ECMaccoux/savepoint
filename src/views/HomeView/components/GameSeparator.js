import React from 'react';
import { View } from 'react-native'
import styles from '../styles';

export default function GameSeparator() {
    return (
        <View style={styles.gameSeparator} />
    );
}